# 贡献指南 | Contributing

当前开发版本是 2.5.0 候选，尚未发布。欢迎提交具体的改写问题、误改案例和文档修正。

## 运行文件

完整 skill 只有三个文件：

- [SKILL.md](SKILL.md)：主要编辑边界和交付要求，单独使用即 lite。
- [references/editing-guide.md](references/editing-guide.md)：容易误改的语义边界，按需查阅。
- [references/examples.md](references/examples.md)：改写对照，按需查阅。

[mini](dist/shuorenhua-mini.md) 是 1,500 字符以内的自包含压缩版，不参与 skill 自动发现。评测、历史记录和开发脚本不属于运行规则。

## 提交具体问题

优先用 [bad case 模板](.github/ISSUE_TEMPLATE/bad-case.md)，附上：

- 原文、用户实际提出的编辑要求、模型交付的全文。
- 使用的模型、规则版本，以及 mini / lite / full 加载方式。
- 哪处表达仍然不自然，或哪条事实、关系、立场被改坏。
- 你认为合理的处理方向；不能确定时说明疑点即可。

不需要先给问题归类或定级。“这个词像 AI”本身不足以新增规则：请说明它在这段话里有什么问题，并给一个相近但不该改的例子。

只提交有权公开的材料。私聊和内部文档先获得授权并脱敏；不要把账号、密钥或其他人的个人信息放进公开 issue。

## 改规则与示例

先判断能否通过更清楚的边界或一组正反例解决问题，不把每个同义词都加入禁用清单。

规则修改应说明：原来在什么请求下会失败、修改后应该怎样处理、相近的正常文本为什么仍然保留。新的例子中，改写稿的每个事实和关系都必须能回指原文，不能靠给“好版本”补数据让对照成立。抽象信息、情态、否定和作者立场也要保留。

只改入口不足以表达的边界时，更新 editing-guide；需要展示不同处理结果时，更新 examples。不要为了分类整齐增加目录、固定场景流程或必读文件。

## 评测用例

当前判分合同见 [v2.5 criteria](evals/v2.5/criteria.md)，公开用例见 [suite.json](evals/v2.5/suite.json)。用例要有明确请求、完整原文与可核对的预期，既覆盖该改的情况，也覆盖不该误改的情况。

保真、任务完成和表达质量分别判断。保真失败要有原文与输出的对应证据；不确定的项留待复核，不能当通过。完整覆盖不能代替效果达标，质量改善也不能抵消语义变化。

不要把校准或开发样本混进独立验收集。旧 benchmark、结果页和原始失败记录用于追溯，不能随新规则改成“当时已经通过”。

## PR 与检查

1. 说明具体问题和修改后的行为，附必要的正反例。
2. 同步受影响的入口、参考文件、安装说明或 mini；不要改无关文件。
3. 规则或判分改动按当前评测合同验证，并记录实际候选、模型、范围与结果。未验证的内容明确标出。
4. 提交前运行 `python3 automation/check_repo.py`；改脚本时再运行 `python3 -m unittest discover -s automation/eval -p 'test_*.py'`。
5. 更新 CHANGELOG，区分未发布候选与已经发布的版本。

过程文档、原始模型输出和临时方案只放在 gitignored 的 `tasks/`。安装包只同步运行文件，不将开发仓库整体放进 skills 扫描目录。提交不加 AI 署名行。

社区讨论可以提供观察线索，但多人嫌弃一个词不等于所有语境都该删除。引用观察时附可核对的公开来源，补上放行边界和反例；不要未经授权搬运全文。
