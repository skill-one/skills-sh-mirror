interface:
  display_name: "Ship It"
  short_description: "When asked to ship a change or open a PR"
  default_prompt: "Use $ship-it to publish this change as a non-draft PR, complete configured review, add verification evidence, and drive CI green."
