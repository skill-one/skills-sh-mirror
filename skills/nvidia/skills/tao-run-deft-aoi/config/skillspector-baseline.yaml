version: 2
# Reviewed false positives — NVIDIA/nvskills-ci#67. No content removed.
rules:
  - id: "TP*"
    path: "SKILL.md"
    reason: "Reviewed FP (#67): the description accurately states the skill purpose; no deceptive tooling."
  - id: "P*"
    path: "agents/reporter.md"
    reason: "Reviewed FP (#67): Apache-2.0 license header / report section markers in HTML comments, not injected instructions."
  - path: "eval.config"
    message: "*rm -rf*"
    reason: "Reviewed FP (#67): scoped/standard command in setup or eval content; not malicious."
  - id: "PE*"
    path: "eval.slow-manual.config"
    reason: "Reviewed FP (#67): standard infra path/flag refs in docs (SSH/kubeconfig/keyring/etc-passwd/docker), not privilege escalation."
  - id: "P*"
    path: "references/DEFT_Loop_Report.html"
    reason: "Reviewed FP (#67): Apache-2.0 license header / report section markers in HTML comments, not injected instructions."
  - id: "PE*"
    path: "references/data-layout.md"
    reason: "Reviewed FP (#67): standard infra path/flag refs in docs (SSH/kubeconfig/keyring/etc-passwd/docker), not privilege escalation."
  - id: "PE*"
    path: "references/paidf-anomalygen.md"
    reason: "Reviewed FP (#67): standard infra path/flag refs in docs (SSH/kubeconfig/keyring/etc-passwd/docker), not privilege escalation."
  - id: "LP*"
    path: "scripts/align_token_usage.py"
    reason: "Reviewed FP (#67): declared capability matches the skill's documented required function."
  - id: "LP*"
    path: "scripts/analyze_kpi.py"
    reason: "Reviewed FP (#67): declared capability matches the skill's documented required function."
  - id: "LP*"
    path: "scripts/init_deft_state.py"
    reason: "Reviewed FP (#67): declared capability matches the skill's documented required function."
  - id: "AE*"
    path: "*"
    reason: "Reviewed FP: SkillSpector 2.11.0 (SkillEvaluator 1.5.4+) introduced hard runtime budgets (MAX_WORKFLOW_SECONDS=60) that cannot be raised externally; large skills are only partially inspected and the partial read is reported as evasion. Upstream: nvcarps-ci!252. Artifacts are present, small and plain-text; nothing is hidden from analysis."
  - id: "skillspector_execution_failure"
    path: "*"
    reason: "Reviewed FP: scan aborts via the same 2.11.0 runtime budget and on reference_resolution of slash-containing prose and template paths (PASS/NO_PASS, schemas/<action>.schema.json, nvcr.io image URIs, frontmatter version strings). Not a property of the skill content. Upstream: nvcarps-ci!252."
  - id: "PE3"
    path: ".env.example"
    reason: "Reviewed FP (#67): placeholder env keys in an example file; no real credentials."
  - id: "TM2"
    path: "references/visual-changenet.md"
    reason: "Reviewed FP (#67): documented model workflow reference, not tool poisoning."
  - id: "E2"
    path: "scripts/deft_exec.py"
    reason: "Reviewed FP (#67): os.environ copy inside the skill's own tests/runner; the environment stays in-process and is not transmitted."
  - id: "LP1"
    path: "scripts/commit_stage.py"
    reason: "Reviewed FP (#67): MCP least-privilege rule applied to a non-MCP skill. The TAO skill schema declares tooling via allowed-tools and has no permissions field, so the capability cannot be declared."
  - id: "P3"
    path: "scripts/init_deft_state.py"
    reason: "Reviewed FP (#67): documentation/logging wording matched as prompt injection; no injected instructions are present."
  - id: "PE3"
    path: "SKILL.md"
    reason: "Reviewed FP (#67): documented infrastructure path reference (e.g. ~/.docker/config.json, ~/.ssh keys) appearing in documentation or example config; the skill performs no credential access."
  - id: "PE3"
    path: "references/preflight.md"
    reason: "Reviewed FP (#67): documented infrastructure path reference (e.g. ~/.docker/config.json, ~/.ssh keys) appearing in documentation or example config; the skill performs no credential access."
  - id: "PE3"
    path: "references/visual-changenet.md"
    reason: "Reviewed FP (#67): documented infrastructure path reference (e.g. ~/.docker/config.json, ~/.ssh keys) appearing in documentation or example config; the skill performs no credential access."
  - id: "PE3"
    path: "tests/test_runtime_hygiene.py"
    reason: "Reviewed FP (#67): documented infrastructure path reference (e.g. ~/.docker/config.json, ~/.ssh keys) appearing in documentation or example config; the skill performs no credential access."
  - id: "PE5"
    path: "references/tao-generate-anomalies.md"
    reason: "Reviewed FP (#67): documented container runtime flag in a usage example; required for GPU/dataloader workloads and not a privilege escalation performed by the skill."
  - id: "PE5"
    path: "references/visual-changenet.md"
    reason: "Reviewed FP (#67): documented container runtime flag in a usage example; required for GPU/dataloader workloads and not a privilege escalation performed by the skill."
  - id: "TM2"
    path: "references/prepare-for-inference.md"
    reason: "Reviewed FP (#67): scoped maintenance command operating on the skill's own workspace or eval fixture; not tool misuse."
  - id: "TM2"
    path: "references/tao-generate-anomalies.md"
    reason: "Reviewed FP (#67): scoped maintenance command operating on the skill's own workspace or eval fixture; not tool misuse."
