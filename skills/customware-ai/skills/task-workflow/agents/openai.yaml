interface:
  display_name: "Task Workflow Skill"
  short_description: "Repeatable gated workflow for task execution with managed Playwright lifecycle"
  default_prompt: "Use $task-workflow to execute the task through artifact-backed phases, Phase 3 check/lint validation, Phase 4 unit coverage, managed Playwright verification, Phase 6 E2E coverage, and final signoff."
