Describe the problem and resulting behavior, with a concrete example if useful.

- Validation run and relevant result:
- Visual changes: before/after, text completeness and intended reading size:
- Compatibility or remaining limits:

Confirm that changed packaged files and `skills/fireworks-tech-graph` were synced
and checked with `tools/distribution.py`, and that no secrets or local output were
included. Keep historical motion approval evidence separate from new snapshots.
